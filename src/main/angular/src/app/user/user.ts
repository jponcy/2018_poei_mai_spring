export class User {
  // firstname: string;
  // lastname: string;
  // username: string;
  // grade: string;
  // id: number;

  constructor(public firstname?: string, public lastname?: string, public username?: string, public grade?: string, public id?: number) {}
}
